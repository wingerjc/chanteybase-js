{
    "create": [
        "CREATE TABLE IF NOT EXISTS collection (",
        "id $TEXT PRIMARY KEY,",
        "title $TEXT NOT NULL,",
        "volume $INT NOT NULL,",
        "publication_year $INT NOT NULL,",
        "collector_id $TEXT NOT NULL",
        ");"
    ],
    "constraints": [
    ]
}