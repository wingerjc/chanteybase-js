'use strict';

describe('chanteyBase.collections module', function() {

  beforeEach(module('chanteyBase.collections'));

  describe('collections view controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var collectionsViewController = $controller('CollectionsViewController');
      expect(collectionsViewController).toBeDefined();
    }));

  });
});
