'use strict';

describe('chanteyBase.songs module', function() {

  beforeEach(module('chanteyBase.songs'));

  describe('Songs view controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var songsViewController = $controller('SongsViewController');
      expect(songsViewController).toBeDefined();
    }));

  });
});
